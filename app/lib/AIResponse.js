export const AIResponse = async (Message, SerivceUserd) => {
    try {
    return 'ai will reponse as soon as possible please wait...'
    } catch (error) {
        console.log('something wents wrong.', error)
    }
}

const HereGetBestSystemPromptBaseOnService = {
   
}